# Sakthi Dental Clinic - Appointment Booking
Appointment popup + MongoDB storage + protected admin dashboard.
Setup: `npm install`; create `.env` from `.env.example`; run `npx vercel dev`.
Deploy on Vercel and add MONGODB_URI, ADMIN_EMAIL, ADMIN_PASSWORD and JWT_SECRET as environment variables.
Admin page: `/admin.html`.
